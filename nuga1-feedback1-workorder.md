# 누가일등 — 추가지시 1차 (PPT 피드백 10건 → 차수 17~20)

> 근거: nuga1_추가지시1.pptx (실사용 스크린샷 10장). 프로젝트에 docs/feedback/nuga1_추가지시1.pptx로 보관 권장.
> **실행 순서: 차수 17(버그)이 최우선 — 점검표 차수 14~16보다 먼저.** 이후 18 → 19 → 20.
> 각 항목 끝 [S#] = 슬라이드 번호.

---

## 차수 17 — 🔴 긴급 버그 일괄 (기능 불능 5건)

1. **본선(라운드 단계) 선수별 스코어 입력 불가** — 진출자 선발로 넘어온 단계에서 선수별 입력이 동작하지 않음. 진출자 명단·조편성·stage_no 연결 어디서 끊기는지 원인 규명 후 수정. 재현: 2단계 대회 → 예선 마감 → 결승에서 선수별 입력 시도 [S10]
2. **조별 입력에서 7조·8조 선택 불가** — 조 선택 UI가 일부 조로 이동 안 됨(스크롤/터치 영역/렌더링 한계 추정). 조 수 많은 대회(19조+)로 재현 확인 [S9]
3. **코스 선택 잘림·탭 이동 불가** — 스코어입력의 코스 선택 칩이 화면에서 잘리고 탭 전환도 안 됨. 4코스 대회로 재현 [S7]
4. **조편성이 버튼 없이 자동 실행됨** — 확정 스펙(빈 화면+[조편성하기] 버튼 → 클릭 시 실행)과 다르게 진입만 해도 자동 편성. 버튼 방식으로 복원, 편성 전 상태 유지 [S5]
5. **홈 히어로 배지 로고 컬러 미표시** — 로고가 어두운 덩어리로만 보임(심벌 옐로우·워드마크 화이트 소실). SVG fill/폰트 로드 확인, brand/logo-badge 원본과 대조. 워드마크 900 유지 [S1]
- 검증: 각 건 재현 시나리오 통과 스크린샷(또는 preview 측정) 보고. **이 차수에서는 버그 수정만, 구조 변경 금지.**

## 차수 18 — 🟡 디자인 회귀 정합 (2건)

1. **서브탭이 확정 A안과 다름** — design/nuga1-subtab-mockup.html A안(아이콘 언더라인 7탭: 집·문서·사람들·명찰·연필·전광판·트로피, 균등 폭, 선택=딥블루+밑줄, 스코어 탭 빨간 점) 기준으로 재구현. 현재 구현과 목업의 차이점을 먼저 목록으로 보고 후 수정 [S3]
2. **헤더의 원본 링크 노출 제거** — 관리공간 헤더(앱바)에 URL 텍스트가 그대로 노출됨. 헤더에서 제거하고 링크 접근은 관리홈 도구 행(전광판·신청 링크 공유)과 각 탭의 [공유] 버튼으로만. 헤더는 대회명+상태 부제만 [S2]

## 차수 19 — 🔵 참가자 구조 개편 (2건)

1. **참가자 추가: 개인/단체 분리** — [참가자 추가] 진입 시 [개인 추가 | 단체 추가] 선택 탭. 개인=기존 폼(전화 필수). 단체=여러 명 일괄 입력(기존 단체 신청 파서 재사용: 이름·전화 붙여넣기 인식). 완료 후 참가자 탭은 확정 명단 리스트로 복귀 [S4]
2. **단계별 명단 구조** — 다단계 대회에서 다음 단계 시작 후 참가자 탭:
   - 기본 명단 = **현재 단계 명단**(예: 결선 진출자). 예선 명단은 세그먼트에 [예선 명단] 별도 탭으로 조회
   - 결선 명단에 **추가/삭제 가능**(불참자 제외, 예비 콜업 — 삭제 시 사유 기록)
   - **[결선 명단 확정]** 버튼 → 확정 후 조편성 가능(확정 전 조편성 버튼 비활성+안내). 흐름: 진출자 자동 선발 → 명단 조정 → 확정 → 조편성 [S8]

## 차수 20 — 🔵 선수별 입력: 코스 구조 (1건)

- **다코스 대회의 선수별 그리드 재구성** [S6]:
  - 코스 2개 이상: 선수 선택 후 **코스 칩 먼저 선택** → 해당 코스의 홀 그리드 표시
  - 18홀 코스는 **전반 9홀 / 후반 9홀 두 섹션**으로 분리 + 각 소계(전반 N·후반 N) + 코스 합계
  - 상단에 전체 합산 스코어(코스 소계 나열 + 총합) 고정 표시
  - 9홀 코스는 섹션 분리 없이 단일 그리드. 1코스 대회는 코스 선택 생략(현행)
  - 차수 17-3(코스 칩 잘림)과 같은 영역이므로 17 수정 결과 위에서 작업
- 검증: 4코스(72홀) 대회에서 코스 전환 입력 → 합산 → 리더보드 반영

---

## 하지 말 것
- 차수 17에서 구조 변경 섞기 (버그만)
- 서브탭 재구현 시 목업과 다른 임의 변형
- 명단 확정 로직을 단계 마감 로직과 중복 구현 (기존 전환·확정 체계 재사용)

## 완료 시
차수 20 후 → 보류했던 점검표 차수 14~16(개인정보·오프라인·보안★) 진행 → 리허설 → 파일럿.
